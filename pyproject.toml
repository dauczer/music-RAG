[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
# LLM prompt strings are inherently long; suppress E501 for prompt-heavy files
"rag/chain.py" = ["E501"]
